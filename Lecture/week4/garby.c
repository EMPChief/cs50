#include <stdio.h>

int main()
{
    int x; // Uinitialisert variabel

    // Forsøker å skrive ut verdien av den uinitialiserte variabelen
    printf("%d\n", x); // Skriver ut en tilfeldig verdi (søppelverdi)

    return 0;
}